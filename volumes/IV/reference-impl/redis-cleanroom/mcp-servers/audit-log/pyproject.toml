[project]
name = "redis-cleanroom-audit-log"
version = "0.1.0"
description = "MCP server: append-only audit log over SQLite (WAL)."
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0",
]

[tool.uv]
package = false
